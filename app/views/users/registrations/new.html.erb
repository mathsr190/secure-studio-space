<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "users/shared/error_messages", resource: resource %>

  <p>団体名</p>
  <div class="field">
    <%= f.text_field :group_name, autofocus: true, placeholder:"例)おもしろグループ" %>
  </div>
  <p>団体名(かな)(ひらがな)</p>
  <div class="field">
    <%= f.text_field :group_name_kana, placeholder:"例)おもしろぐるーぷ" %>
  </div>
  <p>団体代表者(予約管理者)の情報を聞きます</p>
  <p>団体代表者(予約管理者)苗字</p>
  <div class="field">
    <%= f.text_field :last_name, placeholder:"例)確保" %>
  </div>
  <p>団体代表者(予約管理者)名前</p>
  <div class="field">
    <%= f.text_field :first_name, placeholder:"例)太郎" %>
  </div>
  <p>団体代表者(予約管理者)苗字(かな)</p>
  <div class="field">
    <%= f.text_field :last_name_kana, placeholder:"例)かくほ" %>
  </div>
  <p>団体代表者(予約管理者)名前(かな)</p>
  <div class="field">
    <%= f.text_field :first_name_kana, placeholder:"例)たろう" %>
  </div>
  <p>団体代表者(予約管理者)誕生日</p>
  <div class="field">
    <%= f.date_field :date_of_birth, min: "1900-01-01",  max: Date.today %>
  </div>
  <div>電話番号</div>
  <%= f.telephone_field :phone_number, id:"phone-number", placeholder:"例)09012345678", maxlength:"11"%>

  <div>団体所属住所(もしくは団体代表者住所)　郵便番号(半角、ハイフンを入れてください)</div>
  <%= f.text_field :postal_code, id:"postal-code", placeholder:"例)123-4567", maxlength:"8"%>
  <div>都道府県</div>
  <%= f.collection_select(:prefecture_id, Prefecture.all, :id, :name, {include_blank: "---"},{id:"prefecture"})%>
  <div>市区町村</div>
  <%= f.text_field :city, id:"city", placeholder:"例）札幌市緑区"%>
  <div>それ以降の住所</div>
  <%= f.text_field :address, id:"addresses", placeholder:"例）緑山葱背負鴨8-4-4"%>
  <div>建物名(任意)</div>
  <%= f.text_field :building, id:"building", placeholder:"例）すげーと103"%>

  <div class="field">
    <p>以下ログイン時に使います。</p>
    <%= f.label :email %><br />
    <%= f.email_field :email, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign upusers!!" %>
  </div>
<% end %>

<%= render "users/shared/links" %>

